PORT=3000
BASE_URL=http://localhost:3000
# Set DATABASE_URL to a Postgres connection string when using Postgres (leave empty to use local SQLite)
# DO NOT commit real credentials into the repository. Use your hosting provider's env vars (Render/Vercel/Neon).
# Example (replace placeholders):
# DATABASE_URL=postgresql://<USERNAME>:<PASSWORD>@<HOST>:5432/<DBNAME>?sslmode=require&channel_binding=require
DATABASE_URL=
